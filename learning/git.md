# Git Practice Notes

## What I Did Today

1. Went to the D drive.
2. Created the `SPL-1` project folder.
3. Used `git init` to create a Git repository.
4. Used `git status` to check the repository status.
5. Changed the branch name from `master` to `main`.
6. Created a `README.md` file.
7. Used `git add README.md` to stage the file.
8. Used `git commit` to create the first commit.
9. Used `git log --oneline` to see the commit history.
10. Created the `SPL-1` repository on GitHub.
11. Connected the local repository to GitHub using `git remote add origin`.
12. Used `git remote -v` to check the remote repository.
13. Used `git push -u origin main` to upload the local repository to GitHub.
14. Used `git pull` to check for changes from GitHub.
15. Used `git fetch` to fetch remote updates.
16. Used `git branch -vv` to check the upstream branch.
17. Used `git clone` to create a local copy of the GitHub repository.
18. Used `git status` and `git remote -v` in the cloned repository.
19. Created a `Git` folder inside the `SPL-1` project.
20. Created `git-notes.md` inside the `Git` folder.

## Commands I Practiced

```bash
cd
mkdir
touch
git init
git status
git branch -M main
git add
git commit
git log --oneline
git remote add origin
git remote -v
git push -u origin main
git push
git pull
git fetch
git branch -vv
git clone
```

## Project Structure

```text
SPL-1/
├── README.md
└── Git/
    └── git-notes.md
```

